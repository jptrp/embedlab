# Development/test requirements (minimal)
# Use this to create a dev environment for running tests quickly.
pytest==8.4.2
numpy==2.3.4

# Runtime/image deps (optional, not required for running tests):
# torch
# torchvision
# Pillow

# Dev tooling
mypy==1.10.1
ruff==0.12.1
black==26.3.1
# Annoy and scikit-learn are optional and not pinned here because they may require a C/C++ toolchain to build on some platforms (macOS).
# If you need ANN (Annoy) or PCA/metrics (scikit-learn), install them manually in your environment, e.g.:
#   .venv/bin/python -m pip install annoy scikit-learn
